export namespace API_ROUTES {
  export enum GROCERY {
    GET_PRODUCTS = 'products',
    GET_PRODUCT = '/products/:id',
    CREATE_PRODUCT = 'products',
    DELETE_PRODUCT = 'products/:id',
    EDIT_PRODUCT = '/products/:id',
    GET_TAGS = 'tags',
  }
}
